# Point this at your deployed backend before building for the Play Store.
# For local development on a physical device, use your computer's LAN IP,
# not localhost (the phone can't resolve your dev machine's localhost).
EXPO_PUBLIC_API_URL=http://localhost:4000
